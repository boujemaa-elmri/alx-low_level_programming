#include "main.h"
/**
 * main - prints 10 times the alphabet, in lowercase
 *
 * Return: Always 0.
 */
void print_alphabet_x10(void);
{
    int i;
    int j;
    for(j = 1; i <= ; i++)
    {
      for(j = 97 ; j  <= 122; j++ )
      {
        _putchar(i);
      }
    }
     _putchar(i);
}
