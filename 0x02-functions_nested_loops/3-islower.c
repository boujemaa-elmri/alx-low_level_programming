#include "main.h"
/**
 * main - function that checks for lowercase character.
 * Return: Always 0.
 */
int _islower(int c)
{
  if(c >= 97 ; && c <= )
  {
    return (1);
  }
  else
  {
    return (0);
  }
  _putchar('/n');
}
