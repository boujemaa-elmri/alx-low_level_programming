 #include "main.h"
 /**
 * main - function that checks for alphabetic character.
 *
 * Return: 1 for lettre 0 for rest.
 */
int _islower(int c)
{
  if(c >= 97 ; && c <= )
  {
    return (1);  
  }
  else
  {
    return (0);
  }
  _putchar('/n');
}

