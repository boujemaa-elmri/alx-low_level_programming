#include "main.h"
 /**
 * main - function that computes the absolute value of an integer.
 *Returns :absolute value
 */
int _abs(int)
{
  if(i >= 0 )
  {
    return (i);  
  }
  else
  {
    return (i * -1);
  }
  
}
